# Core Schema Assets

The canonical data model is defined in `specs/data-model.md`. Executable Supabase schema bootstrap for the platform core currently lives in:

- `supabase/migrations/20260314113000_core_platform.sql`

This package directory is reserved for extracted SQL modules and generated types as implementation expands.

